# This configuration is generated by matugen
# ~/.config/matugen/templates/hyprland/colors.conf
# Changing these variables with matugen still enabled will overwrite them.

general {
    col.active_border = rgba(909097AA)
    col.inactive_border = rgba(46464cAA)
}

misc {
    background_color = rgba(131315FF)
}

plugin {
    hyprbars {
        bar_color = rgba(131315FF)
        col.text = rgba(e5e2e3FF)
        inactive_button_color = rgba(e5e2e3FF)
    }
}

# macos colors
# $COLOR_BAR_CLOSE = rgb(ed6a5f)
# $COLOR_BAR_MAX = rgb(f8b350)
# $COLOR_BAR_MIN = rgb(61c555)

$COLOR_BAR_CLOSE = rgb(c1dfcb)
$COLOR_BAR_MAX = rgb(c5c6d1)
$COLOR_BAR_MIN = rgb(dcbed3)

windowrule = match:pin 1, border_color rgba(c1c5dfAA) rgba(c1c5df77)
